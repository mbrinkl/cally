{
  "name": "@cally/server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown index.ts --platform node --format esm --out-dir ../../dist",
    "dev": "cross-env NODE_ENV=development tsx watch --env-file=../../.env index.ts"
  },
  "dependencies": {
    "@cally/shared": "*",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "tsdav": "^2.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.3",
    "tsdown": "^0.22.14",
    "tsx": "^4.23.12"
  }
}
