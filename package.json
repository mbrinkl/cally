{
  "name": "@cally/root",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --workspace @cally/client\" \"npm run dev --workspace @cally/server\"",
    "build": "npm run build --workspace @cally/client && npm run build --workspace @cally/server",
    "preview": "npm run preview --workspace @cally/client",
    "prod": "npm run build && npm run server:prod --workspace @cally/server"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "tsup": "^8.5.1"
  }
}
