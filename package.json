{
  "name": "@cally/root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rimraf dist && npm run build --workspace @cally/client && npm run build --workspace @cally/server",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --workspace @cally/client\" \"npm run dev --workspace @cally/server\"",
    "preview": "npm run preview --workspace @cally/client",
    "prod": "npm run build && node --env-file=.env ./dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.1"
  }
}
